// Global variables
gString = "A global String";

/**
 * Does nothing.
 */
function doNothing() {
	
}

/**
 * Displays a String in a alert box.
 * 
 * @param aString String to display
 * @return <code>null</code>
 */
function displayMessage(aString) {
	
	alert(aString);
}